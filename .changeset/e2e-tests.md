---
---

Add end-to-end CI tests that run the add-on image against a real mosquitto broker to verify MQTT metrics ingestion and OTLP/HTTP ingestion both reach the file exporter. Test-only; no change to the shipped add-on.
