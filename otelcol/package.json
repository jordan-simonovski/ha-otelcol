{
  "name": "otelcol-addon",
  "version": "0.2.0",
  "private": true,
  "description": "Home Assistant OpenTelemetry Collector add-on (version mirror for changesets; the add-on itself is built from config.yaml/Dockerfile)"
}
